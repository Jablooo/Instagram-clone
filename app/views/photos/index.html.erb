<div class="container">
  <div class="row col-8">

    <h1>Photos</h1>

    <!-- <% @photos.each do |photo| %>
      <div class="col-12">
        <%= image_tag photo.image_url, class: 'photo' %>
      </div>

      <div class="col-12">
        <%= photo.caption %>
      </div>

      <div class="editing col-12 pb-4">
        <%= link_to 'Show', photo %>
        <%= link_to 'Edit', edit_photo_path(photo) %>
        <%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %>
      </div>
      <br>
    <% end %> -->

    <section>
      <% @photos.reverse.each do |photo| %>
        <article>
          <%= link_to photo do %>
            <figure>
              <%= image_tag photo.image_url, class: 'img-fluid', alt: photo.caption %>
            </figure>
          <% end %>
          <p>
            <strong><%= link_to "#{photo.user.email}", user_path(photo.user_id)  %></strong>
            <!-- <strong><%#= link_to photo.user.email, user_path %></strong> -->
            <%= photo.caption %>
          </p>
        </article>
      <% end %>
    </section>

  </div>

  <br>
  <%= link_to 'New Photo', new_photo_path %>
</div>
