<p class="notice"><%= notice %></p><p class="alert"><%= alert %></p>

<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <%= link_to 'Ninjagram', photos_path, class:'navbar-brand'%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to 'Home', photos_path, class:'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to 'Add New Photo', new_photo_path, class:'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to 'My feed', feed_path, class:'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to 'View Profile', profile_path, class:'nav-link'%>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to('Logout', destroy_user_session_path, method: :delete, class:'nav-link') %>
        <% else %>
          <%= link_to('Login', new_user_session_path, class:'nav-link')  %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
